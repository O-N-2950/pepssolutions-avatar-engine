{
  "name": "@peps/voice-avatar-bridge",
  "version": "0.1.0",
  "description": "Connects voice-core WebSocket to avatar lip-sync — PCM Cartesia → AudioAnalyzer → expression driver",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch"
  },
  "devDependencies": {
    "tsup": "^8.0.2",
    "typescript": "^5.4.5"
  },
  "files": ["dist"],
  "license": "MIT"
}
